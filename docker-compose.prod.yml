services:
  postgres:
    environment:
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
    ports: []

  ovlt-core:
    image: ghcr.io/ovlt/ovlt-core:latest
    environment:
      ENVIRONMENT: production
      DATABASE_URL: "postgresql://ovlt:${POSTGRES_PASSWORD}@postgres:5432/ovlt?sslmode=prefer"
      JWT_SECRET: "${JWT_SECRET}"
      MASTER_ENCRYPTION_KEY: "${MASTER_ENCRYPTION_KEY}"
      TENANT_WRAP_KEY: "${TENANT_WRAP_KEY}"
      CORS_ALLOWED_ORIGINS: "${CORS_ALLOWED_ORIGINS}"
    restart: always
    read_only: true
    tmpfs:
      - /tmp
    command: ["./ovlt-core", "--migrate"]
